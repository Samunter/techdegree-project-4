# techdegree-project-3 Interactive Form

This project utilizes jQuery to add dynamic functionality to a form, validates
form fields, prevents the form from submitting if any fields are invalid, and
visually indicates errors to the user.
